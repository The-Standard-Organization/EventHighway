@namespace EventHighway.Portal.Web.Views.Components.Foundations.Dashboards
@inherits PeriodAwarePanelBase
@using System.Linq
@using EventHighway.Core.Models.Coordinations.HealthChecks.V2
@using EventHighway.Portal.Web.Views.Bases
@using EventHighway.Portal.Web.Services.Views.Foundations.HealthDashboards
@inject IHealthViewService HealthViewService

<Card CssClass="mb-4">
    <HeaderContent>
        <div class="d-flex justify-content-between align-items-center w-100 flex-wrap gap-2">
            <span class="fw-semibold">Duplicate Detection</span>
            <PeriodNavigator Period="EffectivePeriod" WindowStart="EffectiveWindowStart" WindowEnd="EffectiveWindowEnd"
                             Disabled="NavDisabled" CanGoNext="CanGoNext"
                             PeriodChanged="OnPeriodChanged" CustomRangeChanged="OnCustomRangeChanged" PreviousRequested="OnPrevious"
                             NextRequested="OnNext" CurrentRequested="OnCurrent" />
        </div>
    </HeaderContent>
    <ChildContent>
        @if (isLoading)
        {
            <Spinner />
        }
        else if (errorMessage is not null)
        {
            <div class="alert alert-danger mb-0" role="alert">@errorMessage</div>
        }
        else if (summary is not null)
        {
            <div class="row">
                <div class="col-md-4">
                    <div class="row mb-3">
                        <div class="col"><StatTile Variant="StatTileVariant.Amber" Value="@summary.TotalDuplicatesDetected.ToString()" Label="Duplicates" /></div>
                        <div class="col"><StatTile Variant="StatTileVariant.Na" Value="@summary.OverallDuplicateRate.ToString("0.00")" Label="Rate %" /></div>
                    </div>
                    <Chart ChartType="doughnut" Labels="doughnutLabels" Datasets="doughnutDatasets" Height="200" />
                </div>
                <div class="col-md-8">
                    <DataTable TItem="DuplicateDetailV2" Items="details" Columns="columns" Searchable="false" />
                </div>
            </div>
        }
    </ChildContent>
</Card>

@code {
    private bool isLoading = true;
    private string? errorMessage;
    private DuplicateDetectionSummaryV2? summary;
    private List<DuplicateDetailV2> details = new();
    private readonly List<string> doughnutLabels = new() { "Unique", "Duplicates" };
    private List<ChartDataset> doughnutDatasets = new();

    private readonly List<DataTableColumn<DuplicateDetailV2>> columns = new()
    {
        new DataTableColumn<DuplicateDetailV2> { Title = "Address", Value = d => d.EventAddressV2Name },
        new DataTableColumn<DuplicateDetailV2> { Title = "Participant", Value = d => d.EventParticipantV2Name ?? "Unknown" },
        new DataTableColumn<DuplicateDetailV2> { Title = "Total", Value = d => d.TotalEvents },
        new DataTableColumn<DuplicateDetailV2> { Title = "Duplicates", Value = d => d.Duplicates },
        new DataTableColumn<DuplicateDetailV2> { Title = "Rate %", Value = d => d.DuplicateRate.ToString("0.00") },
        new DataTableColumn<DuplicateDetailV2> { Title = "Last Seen", Value = d => d.LastDuplicateSeen?.ToString("u") ?? "—" },
    };

    protected override async Task LoadAsync(TrafficPeriodV2 period, DateTimeOffset windowStart, DateTimeOffset? windowEnd)
    {
        try
        {
            summary = await HealthViewService.RetrieveDuplicateSummaryAsync(period, windowStart, windowEnd);

            details = (summary.ByAddress ?? Enumerable.Empty<DuplicateDetailV2>()).ToList();
            doughnutDatasets = new List<ChartDataset>
            {
                new ChartDataset
                {
                    Label = "Events",
                    Data = new List<double> { summary.TotalUniqueEvents, summary.TotalDuplicatesDetected },
                    Colors = new List<string> { "#2eb85c", "#e55353" }
                }
            };

            errorMessage = null;
        }
        catch
        {
            errorMessage = "Unable to load duplicate detection.";
        }
        finally
        {
            isLoading = false;
            StateHasChanged();
        }
    }
}
